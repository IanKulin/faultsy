services:
  faultsy:
    image: ghcr.io/iankulin/faultsy
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - .env
    volumes:
      - data:/app/data
    restart: unless-stopped

volumes:
  data:
